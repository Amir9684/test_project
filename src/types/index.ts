export type Todo = {
  id: string;
  describe: string;
  isCompleted: boolean;
};
export type SelectOptTheme = {
  label: string;
  value: string;
  color: string;
};
export type SelectOpt = {
  label: string;
  value: string;
};
